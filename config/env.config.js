const path = require('path');
const dotenv = require('dotenv');
// console.log('printing directory name : ',__dirname)
// const envConfPath = path.join(__dirname, '../.env');

dotenv.config({
    path : '.env'
});